name: One-shot runtime shutdown queue fix

on:
  push:
    branches:
      - fix/ci-baseline-and-nova-branding
    paths:
      - .github/workflows/one-shot-runtime-shutdown-queue.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: fix/ci-baseline-and-nova-branding
          fetch-depth: 0
      - name: Apply runtime shutdown patch
        run: python3 .github/patches/runtime_shutdown_queue.py
      - name: Validate patch
        run: git diff --check
      - name: Commit and remove helpers
        run: |
          git config user.name 'Axymorrsen'
          git config user.email 'Axymosen@outlook.com'
          git rm .github/workflows/one-shot-runtime-shutdown-queue.yml .github/patches/runtime_shutdown_queue.py
          git add app/src/main/kotlin/fuck/andes/agent/runtime/AgentRuntimeService.kt
          git commit -m 'fix: fail queued runs when Runtime service shuts down'
          git push origin HEAD:fix/ci-baseline-and-nova-branding
